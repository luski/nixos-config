if [[ -z $BROWSER ]]; then
  echo "BROWSER not defined"
  exit 1
fi

CODE=$(walker --dmenu --inputonly --placeholder "Code")

if [[ -z $CODE ]]; then
  exit 0
fi

URL="https://starfishstorage.atlassian.net/browse/GUI-${CODE/GUI-/}"

"$BROWSER" "$URL"
