font_family family="JetBrainsMono Nerd Font" style=ExtraLight
bold_font auto
italic_font Victor Mono Italic
bold_italic_font Victor Mono Bold Italic
font_size 12
enable_ligatures yes

tab_bar_style powerline
tab_powerline_style round

editor nvim
background_blur 1
background_opacity 0.7

enabled_layouts tall, fat, stack

allow_remote_control yes
listen_on unix:@mykitty

map ctrl+a>t>t detach_window new-tab
map ctrl+a>t>shift+t detach_window tab-left

map ctrl+. layout_action bias 50 70
map --allow-fallback=shifted,ascii ctrl+shift+l next_layout

map ctrl+a>l next_layout
map ctrl+a>\ launch --cwd=current
map ctrl+a>c launch --cwd=current --type=tab
map ctrl+a>n next_tab

map ctrl+shift+h neighboring_window left
map ctrl+shift+l neighboring_window right

# Keep the conditional mappings after the unconditional ones. The later
# mappings let Neovim receive these keys while vim-kitty-navigator is active.
map ctrl+h neighboring_window left
map ctrl+j neighboring_window down
map ctrl+k neighboring_window up
map ctrl+l neighboring_window right
map --when-focus-on var:IS_VIM=true ctrl+h
map --when-focus-on var:IS_VIM=true ctrl+j
map --when-focus-on var:IS_VIM=true ctrl+k
map --when-focus-on var:IS_VIM=true ctrl+l

modify_font cell_height 6px
modify_font cell_width 102%
